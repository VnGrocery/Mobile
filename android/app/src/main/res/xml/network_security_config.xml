<?xml version="1.0" encoding="utf-8"?>
<!--
  The app talks to its own server over plain HTTP during development: the
  emulator reaches the host at 10.0.2.2 and a phone reaches a laptop by its LAN
  address. Android has blocked cleartext by default since Android 9, so a
  release build silently failed every request while the debug build worked -
  cleartext is enabled only in the debug manifest.

  This permits cleartext to those addresses and nothing else, so a build for a
  demo works without opening the app to plain HTTP against the whole internet.
  A deployment with a real domain should be served over HTTPS and does not need
  any of this.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- The emulator's alias for the machine running it. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- Private ranges, for a phone pointed at a laptop on the same wifi. -->
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
    </domain-config>
</network-security-config>
